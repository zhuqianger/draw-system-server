<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.drawsystemserver.mapper.AuctionMapper">

    <resultMap id="BaseResultMap" type="org.example.drawsystemserver.entity.Auction">
        <id column="id" property="id"/>
        <result column="sessionId" property="sessionId"/>
        <result column="playerId" property="playerId"/>
        <result column="startTime" property="startTime"/>
        <result column="endTime" property="endTime"/>
        <result column="duration" property="duration"/>
        <result column="status" property="status"/>
        <result column="winningBidId" property="winningBidId"/>
        <result column="winningTeamId" property="winningTeamId"/>
        <result column="createTime" property="createTime"/>
        <result column="updateTime" property="updateTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM auction WHERE id = #{id}
    </select>

    <select id="selectActiveByPlayerId" resultMap="BaseResultMap">
        SELECT * FROM auction WHERE playerId = #{playerId} AND status = 'ACTIVE' LIMIT 1
    </select>

    <select id="selectCurrentActive" resultMap="BaseResultMap">
        SELECT * FROM auction WHERE status = 'ACTIVE' ORDER BY startTime DESC LIMIT 1
    </select>

    <select id="selectCurrentActiveBySessionId" resultMap="BaseResultMap">
        SELECT * FROM auction WHERE sessionId = #{sessionId} AND status = 'ACTIVE' ORDER BY startTime DESC LIMIT 1
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM auction WHERE status = #{status} ORDER BY startTime DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM auction ORDER BY startTime DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO auction (sessionId, playerId, startTime, endTime, duration, status)
        VALUES (#{sessionId}, #{playerId}, #{startTime}, #{endTime}, #{duration}, #{status})
    </insert>

    <update id="update">
        UPDATE auction
        <set>
            <if test="playerId != null">playerId = #{playerId},</if>
            <if test="startTime != null">startTime = #{startTime},</if>
            <if test="endTime != null">endTime = #{endTime},</if>
            <if test="duration != null">duration = #{duration},</if>
            <if test="status != null">status = #{status},</if>
            <if test="winningBidId != null">winningBidId = #{winningBidId},</if>
            <if test="winningTeamId != null">winningTeamId = #{winningTeamId},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE auction SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateWinning">
        UPDATE auction SET winningBidId = #{winningBidId}, winningTeamId = #{winningTeamId}, status = 'FINISHED'
        WHERE id = #{id}
    </update>

</mapper>
